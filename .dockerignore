# --- Version Control ---
.git
.gitignore
.github

# --- Build Artifacts (Host-side) ---
# We ignore these because Docker should create its own fresh build/ directory
build/
bin/
obj/
*.elf
*.bin
*.hex
*.map
*.list
*.o
*.d
*.su

# --- Precompiled Headers (Host-side) ---
# Never copy host-compiled .gch files into the container; they are OS-dependent
*.gch

# --- Tooling & Debugging ---
.gdb_history
openocd.log
*.svd
.gdbinit
.ccache/
scripts/makefile/

# --- IDEs & Editor Configuration ---
.nvim/
.lsp/
.clangd/
.clangd
compile_commands.json
.undodir/
*.swp
*.swo
Session.vim
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- OS Junk ---
.DS_Store
Thumbs.db
.direnv/
.envrc
.cache/
vendors/

# --- Docker Specifics ---
# We don't need the Dockerfile or the ignore file inside the container itself
Dockerfile
.dockerignore
